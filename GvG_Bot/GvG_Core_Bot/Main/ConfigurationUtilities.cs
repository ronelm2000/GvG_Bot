using Microsoft.Extensions.Configuration;
using System;
using System.Collections.Generic;
using System.Text;

namespace GvG_Core_Bot.Main
{
    public static class ConfigurationUtilities
    {
        /*
        public static IConfigurationRoot Serialize (this IConfigurationRoot config, out Config outConfig)
        {
            Config newConfig = new Config();
            newConfig.char_prefix = config["char_prefix"][0];
            newConfig.token = 

            return config;
        }
        */
    }
}
